# Phase 291 — Representative long-play browser progression

The Chromium browser suite now drives a representative campaign chain through the real Worker path: tutorial completion, warehouse case discovery, case resolution, delayed grain aftermath, finale readiness, save, and reload restoration. The test seeds only the warehouse suspicion flag after completing the visible tutorial so the existing investigation/case and persistence paths are exercised without bypassing resolution commands.

Verification: `node --check tests/phase255-browser.spec.js`. The Playwright smoke test requires the repository's CI browser-test dependencies.
