# TxtRPG Agent Skills 개발 규칙

## 목적

이 문서는 TxtRPG 프로젝트에 Agent Skills 엔지니어링 워크플로를 적용하기 위한 프로젝트 수준 개발 규칙이다.

## 프로젝트 기준

- 저장소: `i10veU/TxtRPG`
- 기본 브랜치: `main`
- 실행 기준: Microsoft Edge/Chromium의 로컬 `file://` 환경
- 현재 코어: HTML/CSS/Vanilla JS
- 저장: IndexedDB 우선, 기존 localStorage는 마이그레이션/fallback 유지
- 장기 확장: Web Worker, Canvas/OffscreenCanvas
- 서버/API/CDN/외부 라이브러리는 현재 기본 구조에 추가하지 않는다.
- UI는 현재 PC 전용 100×200 초미니 키보드 형태를 유지한다.

## 개발 생명주기

비단순 작업은 다음 순서를 따른다.

1. `context-engineering` — 최신 저장소, 관련 코드, 문서, 직전 커밋을 확인한다.
2. `spec-driven-development` — 요구사항과 완료 조건을 먼저 정의한다.
3. `planning-and-task-breakdown` — 작업을 검증 가능한 단위로 분해한다.
4. `incremental-implementation` — 작은 수직 단위로 구현하고 기존 구조에 통합한다.
5. `test-driven-development` — 변경된 동작을 검증하는 테스트를 작성/실행한다.
6. `browser-testing-with-devtools` — 브라우저에서 실제 런타임 동작을 검증한다.
7. `code-review-and-quality` — 정확성, 회귀, 보안, 유지보수성을 검토한다.
8. `code-simplification` — 동작을 유지하면서 불필요한 복잡성을 제거한다.
9. `git-workflow-and-versioning` — 의미 단위로 커밋하고 변경 이유를 남긴다.
10. `documentation-and-adrs` — 아키텍처나 중요한 설계 결정이 바뀌면 문서화한다.

모든 작업에 모든 Skill을 기계적으로 적용하지 않는다. 작업 성격에 필요한 Skill만 선택하되 검증 단계는 생략하지 않는다.

## 자동 개발 루프

사용자가 별도 작업 단위를 지정하지 않고 TxtRPG의 지속 개발을 요청한 경우:

`최신 상태 확인 → 다음 개발 단위 선정 → 구현 → 문법 검사 → 회귀 테스트 → 브라우저 smoke test → 장기 시뮬레이션/결정론 검증(필요 시) → 실패 시 수정 및 재검증 → Phase 커밋 → 다음 작업 상태 기록`

기존 시스템과 충돌하는 변경은 임의로 진행하지 않는다. 충돌이나 요구사항 모순을 발견하면 해당 지점을 명시하고 우선순위를 확인한다.

## 가정과 범위

비단순 작업을 시작하기 전에 다음을 명시한다.

- 요구사항에 대한 가정
- 기존 아키텍처에 대한 가정
- 변경 범위
- 완료를 판정할 검증 방법

명확하지 않은 요구사항을 임의로 보완하지 않는다.

## 구현 원칙

### 1. 기존 구조 우선

현재 모듈 경계를 존중한다.

- `web/core/`: 상태, 액션, NPC/세력/경제 시뮬레이션
- `web/data/`: 지역/NPC/사건 데이터
- `web/storage/`: IndexedDB 저장 계층
- `web/worker/`: 게임 시뮬레이션
- `web/ui/`: 렌더링/입력

기존 기능과 무관한 리팩터링을 작업에 섞지 않는다.

### 2. 세계 시뮬레이션의 일관성

NPC 자율 행동 → 조직 의사결정 → 경제/관계 변화 → 사건 → 플레이어 선택 → Consequence → 다음 자율 행동의 연결을 유지한다.

Worker와 fallback 경로가 존재하는 경우 동일한 입력에 대해 가능한 한 동일한 게임 결과를 생성해야 한다.

### 3. 데이터 중심 설계

세계관 설정은 가능한 한 데이터로 관리하고 엔진 로직과 분리한다. 새로운 시스템은 기존 상태/저장 스키마와의 호환성을 우선 검토한다.

### 4. 단순성

가장 작은 변경으로 요구사항을 충족한다. 새로운 추상화, 의존성, 모듈은 실제 복잡성을 줄이는 경우에만 추가한다.

### 5. 보안

사용자 입력을 신뢰하지 않는다. HTML 삽입, 명령 해석, 저장 데이터 복원 과정에서 입력 경계를 확인하고 기존 보안 속성을 약화시키지 않는다.

## 검증 기준

최소한 다음을 작업 성격에 맞게 수행한다.

- JavaScript/HTML/CSS 문법 또는 정적 검사
- 기존 회귀 테스트
- 변경 기능의 직접 테스트
- Chromium/Edge 실제 실행 smoke test
- IndexedDB 저장 및 새로고침 복원 검증(저장 관련 변경 시)
- Worker와 fallback 결과 비교(시뮬레이션 변경 시)
- 장기 시뮬레이션 및 결정론 검증(세계 상태/경제/NPC 변경 시)

검증 실패를 성공으로 간주하지 않는다. 실패하면 원인을 국소화하고 수정한 뒤 동일한 검증을 다시 수행한다.

## Git 규칙

- 기본 브랜치는 `main`이다.
- 완료된 개발 단위는 Phase를 명시한 의미 있는 커밋으로 남긴다.
- 코드, 테스트, 문서가 하나의 기능 단위를 구성하면 가능한 한 같은 개발 단위로 묶는다.
- 검증하지 않은 변경을 완료 상태로 기록하지 않는다.
- 기존 커밋을 임의로 재작성하거나 force push하지 않는다.

## Definition of Done

작업은 다음 조건을 만족해야 완료로 본다.

1. 요구사항과 acceptance criteria를 충족한다.
2. 기존 기능의 회귀가 없다.
3. 실제 런타임에서 변경 동작이 확인된다.
4. 필요한 테스트가 통과한다.
5. 설계 변경이 있으면 관련 문서가 갱신된다.
6. 변경 내용과 검증 결과가 Git 커밋에 남아 있다.

## 현재 개발 방향

현재 Phase 270까지의 NPC 개인 관계망, 지역 자원·교역 경제, 충돌/위기→소문→사건 루프를 기준선으로 삼는다.

다음 개발 단위는 최신 코드와 테스트 결과를 다시 확인한 뒤 선정한다. 현재 로드맵상 주요 후보는 사건 인과망, 지역 변화와 장기 NPC 목표의 연결, IndexedDB 엔티티 분리 및 장기 시뮬레이션이다. 후보 중 실제 코드 상태와 회귀 위험을 확인하여 하나의 검증 가능한 단위로 선택한다.
