# TxtRPG Agent Skills 개발 규칙

## 목적

이 문서는 TxtRPG 프로젝트에 Agent Skills 엔지니어링 워크플로를 적용하기 위한 프로젝트 수준 개발 규칙이다.

## 프로젝트 기준

- 저장소: `i10veU/TxtRPG`
- 기본 브랜치: `main`
- 실행 기준: Microsoft Edge/Chromium
- 현재 코어: HTML/CSS/Vanilla JS
- 저장: IndexedDB 우선, 기존 localStorage는 마이그레이션/fallback 유지
- 장기 확장: Web Worker, Canvas/OffscreenCanvas
- 서버/API/CDN/외부 라이브러리는 실제 복잡성을 줄이는 경우에만 추가한다.
- UI는 텍스트 우선이며, 구현상 고정 크기에 묶이지 않는 반응형 구조를 지향한다.

## 개발 생명주기

비단순 작업은 다음 순서를 따른다.

1. `context-engineering` — 최신 저장소, 관련 코드, 문서, 직전 커밋을 확인한다.
2. `spec-driven-development` — 요구사항과 완료 조건을 먼저 정의한다.
3. `planning-and-task-breakdown` — 작업을 검증 가능한 단위로 분해한다.
4. `incremental-implementation` — 큰 패치가 필요하더라도 기능적으로 응집된 수직 단위로 구현한다.
5. `test-driven-development` — 변경된 동작을 검증하는 테스트를 작성/실행한다.
6. `browser-testing-with-devtools` — 브라우저에서 실제 런타임 동작을 검증한다.
7. `code-review-and-quality` — 정확성, 회귀, 보안, 유지보수성을 검토한다.
8. `code-simplification` — 동작을 유지하면서 불필요한 복잡성을 제거한다.
9. `git-workflow-and-versioning` — 의미 단위로 변경을 기록하고 되돌릴 수 있는 상태를 유지한다.
10. `documentation-and-adrs` — 아키텍처나 중요한 설계 결정이 바뀌면 문서화한다.

모든 작업에 모든 Skill을 기계적으로 적용하지 않는다. 작업 성격에 필요한 Skill만 선택하되 검증 단계는 생략하지 않는다.

## 자동화 운영 원칙

TxtRPG는 **인간 승인에 의존하지 않는 검증 중심 자율개발 루프**를 사용한다.

기본 흐름:

`최신 상태 분석 → 작업 선정 → Issue 구체화 → 구현 → 테스트/브라우저 검증 → 실패 원인 수정 → PR 기록 → 검증 통과 시 다음 작업`

Pull Request는 인간 승인 대기 지점이 아니라 **변경 이력·검증 결과·롤백 단위를 남기는 감사 가능한 아티팩트**로 취급한다.

자동화는 다음을 수행할 수 있다.

- 구현 범위 결정 및 다음 개발 단위 선정
- 대규모 패치 수행
- 테스트 추가/수정
- PR 생성 및 필요한 후속 수정
- 모든 필수 게이트 통과 후 merge 또는 다음 작업 진행
- 검증 실패 시 원인 분석과 제한된 재시도
- 완료된 작업과 중복되는 작업의 제거 또는 재정의

단, 다음은 자동화 루프의 명시적 안전장치로 유지한다.

- 검증되지 않은 변경을 성공으로 간주하지 않는다.
- 동일 실패를 무한 반복하지 않는다. 실패마다 원인을 새로 검증하고, 반복 실패는 중단 가능한 상태로 기록한다.
- 자동화가 변경의 의미를 잃었다고 판단하면 추가 구현보다 작업 범위 재정의를 우선한다.
- 데이터 삭제, 저장 포맷 변경, 보안 경계 약화 등 파괴적 변경은 반드시 백업/마이그레이션/회귀 경로를 함께 만든다.
- PR merge 권한이 없거나 저장소 보호 규칙이 요구하는 조건을 충족하지 못하면 이를 성공으로 위장하지 않고 명확한 blocker로 기록한다.

## 품질 우선순위

패치 크기 자체는 품질 지표가 아니다. 다음 요소를 우선한다.

### 1. 세계관의 다양성보다 세계의 상호작용 밀도

새로운 지역/NPC/조직/사건을 단순히 추가하는 것보다 기존 요소 사이의 연결을 늘린다.

권장 연결:

- 지역 ↔ NPC 일정
- 지역 ↔ 자원/경제
- NPC ↔ 조직
- 조직 ↔ 관계/갈등
- 사건 ↔ 원인/결과
- 플레이어 선택 ↔ 지속적 consequence
- 소문/정보 ↔ 사건/탐험
- 아이템 ↔ 인물/지역/역사

새 콘텐츠는 가능한 한 최소 하나 이상의 기존 시스템과 실제 동작으로 연결되어야 한다.

### 2. 단순하지만 고급진 UI

UI는 기능을 늘리는 방향보다 정보 위계와 상호작용 품질을 개선한다.

- 텍스트 우선
- 읽기 쉬운 타이포그래피
- 명확한 위치/시간/상태/로그/입력 계층
- 불필요한 장식과 중복 정보 제거
- 키보드/터치 모두 사용 가능
- 작은 화면과 큰 화면에서 모두 레이아웃이 무너지지 않음
- 패널은 화면을 가리는 것보다 정보 탐색을 돕도록 설계
- 새로운 UI 요소가 기존 정보와 중복된다면 추가보다 통합/제거를 우선

### 3. 무결성

최종 목표는 “작동하는 것처럼 보임”이 아니라 **검증 가능한 무결성**이다.

작업 성격에 맞게 다음을 사용한다.

- JavaScript/HTML/CSS 정적 검사
- 집중 회귀 테스트
- 전체 회귀 테스트
- Chromium/Edge 실제 실행
- 브라우저 smoke 및 주요 사용자 흐름
- IndexedDB 저장/복원/마이그레이션
- Worker/fallback parity
- 장기 시뮬레이션 및 상태 invariant
- 결정론이 요구되는 로직의 반복 실행 비교
- 콘솔 uncaught exception 검사

외부 네트워크/CI 환경 문제와 게임 자체 결함을 구분한다. 환경 실패를 게임 성공으로 처리하지 않는다.

### 4. 구조적 단순성

YAGNI/Ponytail 원칙은 “작은 패치 강제”가 아니라 **실제 복잡성을 낮추기 위한 원칙**으로 적용한다.

다음 조건을 만족하면 큰 패치를 허용한다.

- 하나의 명확한 목적을 가진다.
- 기존 시스템을 확장한다.
- 변경 간 의존관계가 설명 가능하다.
- 테스트 범위가 함께 확장된다.
- 실패 시 되돌릴 수 있다.

반대로 다음 경우는 제거/축소한다.

- 동일 기능을 두 시스템이 중복 구현한다.
- 실제 게임플레이에 영향을 주지 않는 복잡성이다.
- 유지비만 늘고 사용자 가치가 없다.
- 기존 테스트/시뮬레이션을 깨뜨리는 주된 원인이다.
- 더 단순한 기존 구조로 동일 결과를 만들 수 있다.

## 세계 시뮬레이션 원칙

세계는 다음 순환을 유지한다.

`NPC 자율 행동 → 조직 의사결정 → 경제/관계 변화 → 사건 → 플레이어 선택 → consequence → 다음 자율 행동`

새로운 시스템은 이 순환에 편입되도록 설계한다.

가능하면 모든 주요 세계 변화에 다음을 정의한다.

- 발생 조건
- 입력 상태
- 변화
- 지속 시간 또는 영속성
- 다른 시스템에 미치는 영향
- 플레이어가 관찰할 수 있는 정보

Worker와 fallback 경로가 존재하는 경우 동일한 입력에 대해 가능한 한 동일한 게임 결과를 생성한다.

## 데이터 중심 설계

세계관 설정은 가능한 한 데이터로 관리하고 엔진 로직과 분리한다.

새 데이터는 다음을 우선 검토한다.

- 기존 schema와 호환되는가
- normalize/save/load가 가능한가
- 기존 엔티티를 재사용할 수 있는가
- 중복 데이터가 필요한가
- 삭제/마이그레이션 경로가 있는가

## 보안

사용자 입력을 신뢰하지 않는다. HTML 삽입, 명령 해석, 저장 데이터 복원 과정에서 입력 경계를 확인하고 기존 보안 속성을 약화시키지 않는다.

## 자동 품질 루프

각 작업은 다음 게이트를 통과해야 한다.

### RED
변경된 동작에 대한 최소 재현/회귀 테스트를 먼저 준비한다.

### GREEN
최소 구현으로 테스트를 통과시킨다.

### REFACTOR
중복과 불필요한 복잡성을 줄인다.

### VERIFY
전체 회귀, 브라우저, 저장/복원, parity 등 작업별 필수 검증을 수행한다.

### DECIDE
- 모두 통과 → 작업 완료 및 다음 작업 탐색
- 실패 → 실패 원인만 국소적으로 수정
- 반복 실패 → 구현을 계속 늘리지 말고 설계/요구사항을 재평가
- 기존 기능 중복/퇴행 → 해당 변경을 축소하거나 제거

## 자동화된 장기 개발의 추가 원칙

- 작업마다 최신 `main`과 직전 변경을 다시 확인한다.
- 이전 작업의 존재 자체를 다음 작업의 필요성으로 간주하지 않는다.
- “더 많이 추가”보다 “더 의미 있게 연결”하는 것을 우선한다.
- 새 기능은 가능하면 기존 기능을 한 단계 깊게 만드는 방향으로 선택한다.
- 세계관 확장은 정적 설명량보다 시스템적 결과와 발견 가능성을 기준으로 판단한다.
- UI 개선은 시각적 장식량보다 정보 접근 시간과 입력 명확성을 기준으로 판단한다.
- 리팩터링은 기능 개발에 필요한 경우에만 수행한다.
- 기능 제거도 정상적인 개발 수단으로 취급한다.
- 검증을 통과한 이전 기능도 새로운 시스템과의 상호작용에서 회귀할 수 있으므로 전체 흐름을 다시 검증한다.

## Git 규칙

- 기본 브랜치는 `main`이다.
- 모든 의미 있는 개발 단위는 Issue와 Pull Request 이력으로 남긴다.
- 코드, 테스트, 문서가 하나의 기능 단위를 구성하면 가능한 한 같은 개발 단위로 묶는다.
- 검증하지 않은 변경을 완료 상태로 기록하지 않는다.
- 기존 커밋을 임의로 재작성하거나 force push하지 않는다.
- 자동 merge가 가능한 환경에서는 필수 CI와 저장소 보호 규칙을 모두 통과한 경우에만 수행한다.
- merge되지 못한 PR은 실패 원인과 다음 조치가 명확하게 남아 있어야 한다.

## Definition of Done

작업은 다음 조건을 만족해야 완료로 본다.

1. 요구사항과 acceptance criteria를 충족한다.
2. 기존 기능의 회귀가 없다.
3. 실제 런타임에서 변경 동작이 확인된다.
4. 필요한 테스트가 통과한다.
5. 설계 변경이 있으면 관련 문서가 갱신된다.
6. 변경 내용과 검증 결과가 Pull Request 또는 커밋 이력에 남아 있다.
7. 필수 품질 게이트가 모두 통과했다.

## 현재 개발 방향

게임 본체는 기능 추가 자체를 목표로 삼지 않는다. 새로운 작업은 최신 코드와 테스트 결과를 기반으로 선정한다.

Phase 번호는 개발 목표가 아니라 작업 식별자일 뿐이다.

자동화는 다음 개발 단위를 스스로 선정할 수 있으며, 완료 조건을 만족하는 동안 연속적으로 개발할 수 있다.
